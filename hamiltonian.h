#pragma once

#include <vector>
#include <set>

bool hamiltonian(std::vector<std::set<int>> &graph);

// bool is_possible_next_node(std::vector<std::set<int>> &graph, std::vector<int> &visited_nodes, int next_node);